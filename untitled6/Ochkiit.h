#include <fstream>
#include <iostream>
#include <string>
#include "ochki.h"

using namespace std;

namespace Ochkiit{

void _create (Ochki* arr, int n);
Ochki *_load (int &n);
void _save(Ochki* arr, int n);
void _search(Ochki* arr,int n);

void _delete(Ochki* arr,int n);
void _insert(Ochki* arr,int n);
void _add(Ochki* arr,int n);
};
